const express = require("express");
const axios = require("axios");
const fs = require("fs"); // Node built-in — should be ignored
const path = require("path"); // Node built-in — should be ignored
import lodash from "lodash";
import "./someLocalFile"; // local import — should be ignored
